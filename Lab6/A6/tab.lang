int main() {
	int n = 1;
	int guess = 0;
	int nbr = gen(n);
	while(guess != nbr) {
		print(nbr);
		guess = read();
		n = n + 1;
		nbr = gen(n);
	}
	return 0;
}

int gen(int n) {
	return 3 + (n - 1) * 4;
}
